# Kotaiba Salon 💈

A premium, modern, and completely responsive Arabic barber salon landing page built with pure semantic HTML5, Vanilla CSS3, and JavaScript. The project completely avoids UI frameworks like Tailwind or Bootstrap in favor of full custom aesthetic control for a high-end luxury feel.

## 🌟 Key Features

*   **Premium Dark Theme:** Built with a sophisticated charcoal (`#0a0a0a`) background, sleek dark cards (`#141414`), and warm luxury gold (`#d4af37`) accents.
*   **Fully Responsive:** The layout is thoroughly optimized for all device sizes. Mobile views include a sleek hamburger menu interaction and a streamlined single-column layout stack.
*   **Modern Typography:** Uses the 'Tajawal' Arabic Google font for professional, clean, and highly readable modern Arabic text.
*   **Interactive UI/UX:**
    *   **Multi-language Support (Ar/En):** A custom i18n dictionary system paired with dynamic DOM `dir` switching ensures layout and text seamlessly flip between Left-To-Right (English) and Right-To-Left (Arabic).
    *   **Animated Splash Screen:** A beautiful pulse-animated loading screen featuring the salon's logo when users first load the page.
    *   **Hero Slider:** Smooth fading JavaScript image slider to showcase the salon's interior.
    *   **Floating WhatsApp Integration:** A persistent button for immediate booking and customer inquiries.
    *   **Scroll-to-Top:** A smooth-scrolling return button that appears dynamically as the user views content.
    *   **Micro-animations:** Subtle hover lift effects, glows, and transitions across all services and product cards.
    *   **Image Modals:** Clickable product images that open gracefully in a detailed dark-backdrop modal.
*   **Clean Architecture:** Files are meticulously organized: all styles reside in `css/style.css`, all logic in `js/main.js`, and all visual assets in `assets/images/`.

## 📁 Project Structure

```text
Kotaiba-Salo/
├── index.html          # Main HTML structure
├── css/
│   └── style.css       # Core design system and responsive styles
├── js/
│   └── main.js         # Interactive logic (slider, modal, menu, splash)
└── assets/
    └── images/         # Optimized images, logos, and icons
```

## 🚀 Setup & Installation

To run this project, no build tools or package managers are required. It is pure Vanilla code.

1.  Clone this repository to your local machine:
    ```bash
    git clone https://github.com/MahmoudEsawi/Kotaiba-Salon.git
    ```
2.  Navigate to the project directory:
    ```bash
    cd Kotaiba-Salon
    ```
3.  Open `index.html` directly in any modern web browser to view the site. Alternatively, you can use a simple local server like Python's HTTP server:
    ```bash
    python3 -m http.server 8080
    ```
    Then navigate to `http://localhost:8080`.

## 🛠️ Built With

*   **HTML5** (Semantically Structured)
*   **CSS3** (Flexbox, CSS Grid, Custom Properties/Variables, Keyframe Animations)
*   **JavaScript (ES6+)** (Vanilla DOM Manipulation)
*   **FontAwesome** (For sleek iconography)
*   **Google Fonts** (Tajawal)

## 👤 Author

**Kotaiba Salon Team**
- [Direct Booking (WhatsApp)](https://wa.me/962796519748)